/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'source.unsplash.com',
      'res.cloudinary.com',
      'lh3.googleusercontent.com',
    ],
  },
};

module.exports = nextConfig;
